version: '3'

services:
  db:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_HOST: localhost
      POSTGRES_PORT: 5432
      POSTGRES_DB: cas-web
      POSTGRES_USER: cas
      POSTGRES_PASSWORD: cas
